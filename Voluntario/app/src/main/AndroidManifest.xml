<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.pe.continental.vadt.voluntary">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VadtVoluntary">
        <activity android:name=".InicialActivity"></activity>
        <activity android:name=".MenuRutaActivity" />
        <activity android:name=".RegistrarCursoActivity" />
        <activity android:name=".TecnologiaUsadaActivity" />
        <activity android:name=".RutasAprendisajeActivity" />
        <activity android:name=".actividades.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MenuActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>